@model ProfessionalPartnerships.Data.Models.Interests
@{
    ViewData["Title"] = "Edit Interest";
}

<h2>Edit Interest</h2>
@using (Html.BeginForm("EditInterest", "Interests", FormMethod.Post))
{
    @Html.HiddenFor(model => model.InterestId)
    @Html.LabelFor(model => model.Name)
    @Html.TextBoxFor(model => model.Name)

    <input type="submit" value="Edit" />

}

