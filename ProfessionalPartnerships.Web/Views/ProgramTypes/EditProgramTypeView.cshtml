@model ProfessionalPartnerships.Data.Models.ProgramTypes
@{
    ViewData["Title"] = "Edit Program Type";
}

<h2>Edit Program Type</h2>
@using (Html.BeginForm("EditProgramType", "ProgramTypes", FormMethod.Post))
{
    @Html.HiddenFor(model => model.ProgramTypeId)
    @Html.LabelFor(model => model.Name)
    @Html.TextBoxFor(model => model.Name)

    <input type="submit" value="Edit" />

}

