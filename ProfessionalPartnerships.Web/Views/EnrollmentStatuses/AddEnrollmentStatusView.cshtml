@model ProfessionalPartnerships.Data.Models.EnrollmentStatuses
@{
    ViewData["Title"] = "Add Enrollment Status";
}

<h2>Add Enrollment Status</h2>
@using (Html.BeginForm("AddEnrollmentStatus", "EnrollmentStatuses", FormMethod.Post))
{
    @Html.LabelFor(model => model.Name)
    @Html.TextBoxFor(model => model.Name)

    <input type="submit" value="Add" />

}

